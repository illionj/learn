下面按**神经网络文档最常用场景**整理了一份 LaTeX 写法速查表，重点放在公式、矩阵、损失函数和常见符号。

---

# 一、基础数学环境

### 行内公式 / 独立公式

```latex
这是行内公式 $a^2 + b^2 = c^2$

\[
a^2 + b^2 = c^2
\]
```

---

# 二、上下标、分数、根号

```latex
x_i, \quad x^{(k)}

\frac{a}{b}

\sqrt{x}, \quad \sqrt[n]{x}
```

---

# 三、求和、连乘、极限

```latex
\sum_{i=1}^{n} x_i

\prod_{i=1}^{n} x_i

\lim_{x \to 0} f(x)
```

---

# 四、矩阵（神经网络最常用）

### 基础矩阵

```latex
\begin{bmatrix}
a & b \\
c & d
\end{bmatrix}
```

### 向量

```latex
\mathbf{x} = 
\begin{bmatrix}
x_1 \\
x_2 \\
x_3
\end{bmatrix}
```

---

# 五、常见神经网络表达式

### 1️⃣ 线性层（全连接层）

```latex
\mathbf{y} = \mathbf{W}\mathbf{x} + \mathbf{b}
```

---

### 2️⃣ 激活函数

#### ReLU

```latex
\text{ReLU}(x) = \max(0, x)
```

#### Sigmoid

```latex
\sigma(x) = \frac{1}{1 + e^{-x}}
```

#### Tanh

```latex
\tanh(x)
```

---

### 3️⃣ 前向传播

```latex
\mathbf{h}^{(l)} = f\left(\mathbf{W}^{(l)} \mathbf{h}^{(l-1)} + \mathbf{b}^{(l)}\right)
```

---

### 4️⃣ 损失函数

#### 均方误差（MSE）

```latex
\mathcal{L} = \frac{1}{n} \sum_{i=1}^{n} (y_i - \hat{y}_i)^2
```

#### 交叉熵

```latex
\mathcal{L} = - \sum_{i=1}^{n} y_i \log(\hat{y}_i)
```

---

### 5️⃣ Softmax

```latex
\text{softmax}(x_i) = \frac{e^{x_i}}{\sum_j e^{x_j}}
```

---

# 六、导数与梯度（训练核心）

### 普通导数

```latex
\frac{d f(x)}{d x}
```

### 偏导

```latex
\frac{\partial L}{\partial w}
```

### 梯度

```latex
\nabla_{\mathbf{w}} L
```

---

# 七、常见符号写法

```latex
\mathbf{x}      % 粗体向量
\mathcal{L}     % 损失函数
\hat{y}         % 预测值
\theta          % 参数
\epsilon        % 很小的数
```

---

# 八、对齐公式（推荐写论文用）

```latex
\begin{aligned}
y &= Wx + b \\
z &= \sigma(y)
\end{aligned}
```

---

# 九、分段函数（ReLU等）

```latex
f(x) =
\begin{cases}
x, & x > 0 \\
0, & x \leq 0
\end{cases}
```

---

# 十、常见技巧

### 自动缩放括号

```latex
\left( \frac{a}{b} \right)
```

### 省略号

```latex
\cdots \quad \ldots
```

---

# 十一、神经网络论文常见完整例子

```latex
\begin{aligned}
\mathbf{h}^{(1)} &= \sigma(\mathbf{W}^{(1)} \mathbf{x} + \mathbf{b}^{(1)}) \\
\mathbf{h}^{(2)} &= \sigma(\mathbf{W}^{(2)} \mathbf{h}^{(1)} + \mathbf{b}^{(2)}) \\
\hat{y} &= \text{softmax}(\mathbf{h}^{(2)})
\end{aligned}
```

---

如果你需要，我可以帮你整理一份**“神经网络论文模板（IEEE / NeurIPS风格）”**或者把你现有内容直接改成 LaTeX。


$
p= \sigma(z) = \frac{1}{1+e^{-z}}
$


$
L = -\left(y\log p + (1-y)\log(1-p)\right)
$


$
\frac{\partial {L}}{\partial{z}}=\frac{\partial{L}}{\partial{p}} \cdot \frac{\partial{p}}{\partial {z}}
$

$
\frac{\partial {L}}{\partial {p}}=-(\frac {y}{p} - \frac{1-y}{1-p}) 
$

$
\frac{\partial {p}}{\partial{z}}=\frac{e^{-z}}{(1+e^{-z})^2}
$

$
\frac{\partial{L}}{\partial{z}}=-(\frac {y}{p} - \frac{1-y}{1-p}) \cdot \frac{e^{-z}}{(1+e^{-z})^2}
$