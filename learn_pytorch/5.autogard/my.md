雅可比矩阵
每一行,一个输出yi对所有输入的变化率
每一列,某个输入x对所有输出的影响


任何线性映射，在选定坐标系后，都可以表示成一个矩阵

雅可比矩阵的每一行就是yi的梯度
每一个输出分量yi都对应一个关于输入的梯度

当你只改变输入的第 
𝑗
j 个分量 
𝑥
𝑗
x
j
	​

，输出的第 
𝑖
i 个分量 
𝑦
𝑖
y
i
	​

 会以多快的速度变化

偏导 一个标量输出对某个输入分量的变化率

假设有一个函数
$
f:\mathbb{R}^n \to \mathhbb{R}
$
即输入是向量